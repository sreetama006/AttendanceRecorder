<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.globsyn.attendencemaker">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TeacherStudentActivity" />
        <activity android:name=".TeacherActivity" />
        <activity android:name=".TeacherLoginActivity" />
        <activity android:name=".TeacherRegisterActivity" />
        <activity android:name=".QRcodeActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".StudentRegisterActivity" />
        <activity android:name=".StudentLoginActivity" />
        <activity android:name=".StudentLogoutActivity" />
        <activity android:name=".TeacherLogoutActivity" />
        <activity android:name=".StudentCameraActivity" />
        <activity android:name=".AttendenceActivity" />
        <activity android:name=".TeacherAttendencelistActivity" />
        <activity android:name=".VerifyActivity"></activity>
    </application>

</manifest>